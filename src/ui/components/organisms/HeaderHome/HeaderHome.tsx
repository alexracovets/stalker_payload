import { AtomWrapper } from "@atoms";

export const HeaderHome = () => {
    return (
        <AtomWrapper variant="headerHome" asChild>
            <header>

            </header>
        </AtomWrapper>
    );
};