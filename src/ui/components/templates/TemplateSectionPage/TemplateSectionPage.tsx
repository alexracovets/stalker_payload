"use client";

import { Section } from "@payload-types";

interface TemplateSectionPageProps {
  data: Section;
}

export const TemplateSectionPage = ({ data }: TemplateSectionPageProps) => {
  return <div>TemplateSectionPage</div>;
};
